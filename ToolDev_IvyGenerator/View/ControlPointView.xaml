<UserControl x:Class="ToolDev_IvyGenerator.View.ControlPointView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:ToolDev_IvyGenerator.ViewModel"
             xmlns:local="clr-namespace:ToolDev_IvyGenerator.View"
             mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    
    <Grid x:Name="RootCP">
        <Grid.DataContext>
            <viewModels:ControlPointViewModel/>
        </Grid.DataContext>
        <GroupBox Margin="5" Header="Control Point Parameters">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding SplineCPData.Position}"/>
                <TextBlock Grid.Row="1" Text="{Binding SplineCPData.Tangent}"/>
                <!--Put CP stuff in here!-->
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
